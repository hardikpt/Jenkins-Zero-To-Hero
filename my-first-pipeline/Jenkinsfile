pipeline {
    agent {
        docker { image 'node:16-alpine' }
    }
    stages {
        stage('Clone Repo') {
            steps {
                sh 'git clone https://github.com/your-username/your-repo.git'
            }
        }
        stage('Test') {
            steps {
                sh 'node --version'
            }
        }
    }
}
